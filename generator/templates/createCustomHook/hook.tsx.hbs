import * as React from 'react';

export const use{{pascalCase name}} = () => {
  const [state, setState] = React.useState(null);

  React.useEffect(() => {
    // add hooks logic
  }, []);

  return state;
};