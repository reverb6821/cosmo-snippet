import { render } from '@testing-library/react';

import {{pascalCase page}} from '../{{pascalCase page}}';

describe('{{pascalCase page}}', () => {
  test('rendered component', () => {
    const { getByText } = render(<{{pascalCase page}} />);
    expect(getByText('{{pascalCase page}} Page')).toBeInTheDocument();
  });
})