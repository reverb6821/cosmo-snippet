import * as React from 'react';
import { Outlet } from 'react-router-dom'

const {{pascalCase page}}: React.FC = () => {
    return(
        <React.Fragment>
            {{pascalCase page}} Component
            <Outlet />
        </React.Fragment>
)};

export default {{pascalCase page}}