import type { Meta, StoryObj } from '@storybook/react';

import {{pascalCase component}}  from '../{{pascalCase component}}';

const meta: Meta<typeof {{pascalCase component}}> = {
    title: 'Starter/{{pascalCase component}}',
    component: {{pascalCase component}},
  };

export default meta;
type Story = StoryObj<typeof {{pascalCase component}}>;

export const Default: Story = {
    args: {
      // ADD ARGS HERE
    },
  };