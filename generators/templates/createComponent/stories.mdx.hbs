import { Canvas, Meta } from "@storybook/blocks";
import * as {{pascalCase component}}Story from './{{pascalCase component}}.stories.tsx';

<Meta of={ {{pascalCase component}}Story } title="{{pascalCase component}}" />

# {{pascalCase component}}

Description for {{pascalCase component}} component.

<Canvas of={ {{pascalCase component}}Story.Default } />

## Usage

```ts
import {{pascalCase component}} from '@/components/{{pascalCase component}}/{{pascalCase component}};

const Example = () => {
  return (
      <{{pascalCase component}}/>
  );
};

export default {{pascalCase component}};
```

#### Arguments

- **prop title** - prop description
- **prop title** - prop description
- **prop title** - prop description
- **prop title** - prop description
